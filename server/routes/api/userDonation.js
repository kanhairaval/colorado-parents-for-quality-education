const router = require("express").Router(); //Importing the built-in routing functionality from the express module

const UserDonationInfo = require("../../models/UserDonationInfo"); //Importing UserDonationInfo model to create entries in the databse when users make donations

// Post aka create route when a user makes a donation
router.post("/", async (req, res) => {

    
});